/** Minimal body: one chord line + one lyric line. */
export const twoLineBody = `       Em7
Eu sei`;

/** Same excerpt with a section header `[...]`. */
export const sectionedBody = `[Verse]
       Em7
Eu sei`;

/** Two aligned lines (chords and lyrics on the same visual row). */
export const alignedBody = `              A7                   Em7
Que a maré mansa se esconde no escuro`;
